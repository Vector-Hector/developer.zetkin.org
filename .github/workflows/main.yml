name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/reuseable-base.yml
    with:
      test-script: lint

  check-types:
    uses: ./.github/workflows/reuseable-base.yml
    with:
      test-script: check-types
